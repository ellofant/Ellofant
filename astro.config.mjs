import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://alonso510.github.io',
  base: '/astro-app',
  // outDir is not specified, so it will use the default 'dist'
});